<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.gestorgastos">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission
        android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_distribuidora_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_distribuidora_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Recuperar_password" />
        <activity android:name=".perfil_usuario" />
        <activity android:name=".previewFoto" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Maps.MapsActivity2"
            android:label="@string/title_activity_maps2" />
        <activity android:name=".Editar.editar_camion" />
        <activity
            android:name=".Maps.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Editar.editar_usuario" />
        <activity android:name=".Editar.editar_viaje_encamino" />
        <activity android:name=".Editar.editar_viaje_planeado" />
        <activity android:name=".Listas.viajes_propios" />
        <activity android:name=".Add.addViaje" />
        <activity android:name=".Gestores.gestionGastos" />
        <activity android:name=".Add.addCamion" />
        <activity android:name=".Gestores.gestionCamiones" />
        <activity android:name=".Gestores.gestionCategorias" />
        <activity android:name=".Listas.listaConductores" />
        <activity android:name=".Add.addUsuario" />
        <activity android:name=".Listas.listaViajes" />
        <activity android:name=".Listas.listaGastos" />
        <activity android:name=".Roles.adminScreen" />
        <activity android:name=".Add.addGasto" />
        <activity android:name=".Roles.usuarioScreen" />
        <activity
            android:name=".WelcomeActivity"
            android:theme="@style/WelcomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme" />
    </application>

</manifest>